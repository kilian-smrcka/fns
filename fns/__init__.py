"""FNS-style financial news scraper — speed + correctness first.

Source of truth for MVP: SEC EDGAR primary filings (no rumor blogs,
no JS-heavy scraping). All networking is stdlib-only so `python3 -m fns`
works with zero pip installs.
"""

__version__ = "0.1.0"
